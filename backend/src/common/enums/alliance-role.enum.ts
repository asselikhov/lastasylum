/**
 * App account role (JWT `role`, user.role). Not squad ranks — those are R1–R5 only.
 */
export enum AllianceRole {
  MEMBER = 'MEMBER',
  OFFICER = 'OFFICER',
  MODERATOR = 'MODERATOR',
  ADMIN = 'ADMIN',
}
