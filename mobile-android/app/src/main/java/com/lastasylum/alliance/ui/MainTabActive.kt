package com.lastasylum.alliance.ui

import androidx.compose.runtime.compositionLocalOf

/** True when this main bottom-nav tab is the visible tab (not retained at 0×0). */
val LocalMainTabActive = compositionLocalOf { true }
